CREATE TABLE directoryCache (
_id INTEGER PRIMARY KEY NOT NULL,
uri TEXT NOT NULL UNIQUE,
name TEXT
);

selectByUri:
SELECT * FROM directoryCache WHERE uri=?;

selectById:
SELECT * FROM directoryCache WHERE _id=?;

insertEntry:
INSERT OR IGNORE INTO directoryCache VALUES ?;
